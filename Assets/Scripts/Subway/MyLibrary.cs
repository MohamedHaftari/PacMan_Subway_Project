 namespace MyLibrary
{
    public enum MyXdirection
    {
        left = -1, idle, right
    }
    public enum MyYdirection
    {
        down = -1, idle, top
    }
    public enum MyPosition
    {
        left,
        center,
        right
    }
}

